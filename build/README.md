App icons generated from the Mac circular mark
`icon_tile_white_layered_Mac.svg` (colour logo clipped to a circle).

- `icon.png` — 1024×1024, used by electron-builder (Linux / source for macOS)
- `icon.ico` — Windows multi-size icon
- `icon_tile_white_layered_Mac.svg` — source SVG
- `icon_tile_white.png` — linked raster for that SVG
